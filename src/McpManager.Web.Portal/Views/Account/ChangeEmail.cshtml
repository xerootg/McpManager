@using McpManager.Core.Data.Models.Identity
@model McpManager.Web.Portal.Dtos.Account.ChangeEmailDto

@{
    var user = ViewData["User"] as User;
}

<form asp-action="ChangeEmail">
    <div asp-validation-summary="ModelOnly" class="alert alert-error mb-4"></div>

    <div class="grid grid-cols-12 gap-6">
        <!-- Info Cards - Left Column -->
        <div class="col-span-12 lg:col-span-5">
            <!-- User Info Card -->
            <div class="card bg-base-100 border border-base-200 mb-4">
                <div class="card-body p-4">
                    <h3 class="fieldset-legend">Account</h3>
                    <div class="flex items-center gap-4 mt-2">
                        <div class="avatar placeholder">
                            <div class="bg-neutral text-neutral-content w-12 rounded-full flex items-center justify-center">
                                <span class="text-lg">@(user?.GivenName?.Substring(0, 1).ToUpper() ?? "U")</span>
                            </div>
                        </div>
                        <div>
                            <div class="font-medium">@user?.FullName</div>
                            <div class="text-sm text-base-content/60">@user?.Email</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Notes Card -->
            <div class="card bg-base-100 border border-base-200">
                <div class="card-body p-4">
                    <h3 class="fieldset-legend">Before you change it</h3>
                    <ul class="text-sm text-base-content/70 mt-2 space-y-1">
                        <li class="flex items-center gap-2">
                            <icon name="information-circle" size="4" class="text-info" />
                            You sign in with your email address
                        </li>
                        <li class="flex items-center gap-2">
                            <icon name="information-circle" size="4" class="text-info" />
                            Single sign-on matches you by email
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Email Form - Right Column -->
        <div class="col-span-12 lg:col-span-7">
            <div class="card bg-base-100 border border-base-200">
                <div class="card-body p-4">
                    <h3 class="fieldset-legend">New Email</h3>
                    <p class="text-xs text-base-content/60 mb-2">Enter the email address you want to use.</p>
                    <div class="grid grid-cols-12 gap-3">
                        <form-field asp-for="NewEmail" type="email" class="col-span-12" />
                    </div>
                </div>
            </div>

            <partial name="Form/_ActionButtons" />
        </div>
    </div>
</form>
